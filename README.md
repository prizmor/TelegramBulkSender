# TelegramBulkSender

Веб-приложение на ASP.NET Core 8.0 (Razor Pages) для безопасной массовой рассылки сообщений через клиентский аккаунт Telegram. Решение поддерживает роли Root/User, интеграцию с Telegram через WTelegramClient, MySQL, Entity Framework Core и Serilog.

## Возможности

- 🔐 JWT-аутентификация, управление пользователями и ролями
- 🤖 Telegram-клиент на базе WTelegramClient с хранением сессии в файле `telegram.session`
- 💬 Управление чатами, группами чатов и шаблонами сообщений
- 🚀 Отправка двуязычных рассылок с вложениями, логирование результатов
- 📊 История рассылок, удаление сообщений и аудит действий пользователей
- 🧾 Экспортируемые логи и прогресс отправки в режиме реального времени
- 🐳 Контейнеризация через Docker/Docker Compose

## Требования

- .NET 8 SDK
- MySQL 8.x
- Telegram API ID/API Hash (https://my.telegram.org)
- Docker 24+ (опционально)

## Структура проекта
```
TelegramBulkSender/
├── TelegramBulkSender.API/        # ASP.NET Core Web App (Razor Pages)
│   ├── Data/                      # DbContext и миграции
│   ├── Models/                    # EF Core модели
│   ├── Services/                  # Бизнес-логика и Telegram интеграция
│   ├── Pages/                     # Razor Pages + Bootstrap UI
│   ├── wwwroot/                   # CSS/JS/статические файлы
│   └── Program.cs                 # Точка входа
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

## Настройка окружения

1. Скопируйте `.env.example` в `.env` и заполните переменные:
   ```env
   TELEGRAM_API_ID=your_api_id
   TELEGRAM_API_HASH=your_api_hash
   TELEGRAM_PHONE=+10000000000
   ROOT_PASSWORD=strong_password
   JWT_SECRET=super_long_secret
   MYSQL_CONNECTION=Server=mysql;Database=tg_bulk;User=root;Password=password;
   ```
2. Получите API ID/Hash на https://my.telegram.org
3. Первый запуск создаст пользователя `root` с паролем из `ROOT_PASSWORD`

## Локальный запуск (без Docker)

```bash
cd TelegramBulkSender.API
dotnet user-secrets set "MYSQL_CONNECTION" "Server=localhost;Database=tg_bulk;User=root;Password=..."  # опционально
export TELEGRAM_API_ID=...
export TELEGRAM_API_HASH=...
export TELEGRAM_PHONE=...
export ROOT_PASSWORD=...
export JWT_SECRET=...
dotnet ef database update
dotnet run
```
Приложение будет доступно на http://localhost:5000

## Запуск через Docker Compose

```bash
cp .env.example .env
# заполните значения
sudo docker compose up --build
```
Сервисы:
- `app` — ASP.NET Core + Telegram клиент
- `mysql` — MySQL 8.0 (данные в volume `mysql_data`)

Тома:
- `./telegram.session` — файл сессии Telegram
- `./logs` — логи Serilog
- `./uploads` — временное хранение вложений

## Роли и права

| Роль | Возможности |
|------|-------------|
| Root | Управление пользователями, глобальные шаблоны и группы, системные чаты, просмотр всех рассылок и логов |
| User | Создание рассылок, личные группы и шаблоны, просмотр собственной истории |

Сессия JWT обновляется автоматически через refresh-токены, срок жизни access-токена — 24 часа.

## Telegram интеграция

- Используется библиотека [WTelegramClient](https://github.com/wiz0u/WTelegramClient)
- Сессия хранится в файле `telegram.session` (смонтирован в Docker volume)
- При первом запуске понадобится код авторизации Telegram (см. логи контейнера)
- Поддерживается загрузка готового `telegram.session` в корень проекта перед запуском

## База данных

Используется MySQL + EF Core (Pomelo). Миграции применяются автоматически при старте (`Database.Migrate`).

Основные таблицы: пользователи, чаты, группы, шаблоны, рассылки, файлы, пользовательские логи.

## Логирование

Serilog сохраняет логи в `logs/app-.log` (ротация по дням). В Docker каталог логов монтируется наружу. Основные уровни:
- Information — старт приложения, подключение к БД, авторизация Telegram
- Warning — ошибки отправки конкретным чатам, FloodWait
- Error — критические ошибки

## Безопасность

- BCrypt для хеширования паролей
- JWT с хранением токена в HTTP-only cookies
- EF Core защищает от SQL-инъекций
- CSRF/валидация обеспечивается Razor Pages и antiforgery токенами
- Все действия пишутся в `UserLog`

## Разработка

- Razor Pages + Bootstrap 5 + vanilla JS
- Компонентные страницы: Broadcast, Chats, ChatGroups, Templates, BroadcastHistory, Users, UserLogs
- Хост-сервис `TelegramSyncHostedService` периодически синхронизирует список чатов

## Deployment

1. Соберите Docker-образ (`docker build .`)
2. Настройте переменные окружения и тома (telegram.session/logs/uploads)
3. Запустите `docker compose up -d`

